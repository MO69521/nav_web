{
  "read": "auth != null && auth.loginType != 'ANONYMOUS' && doc._id == auth.uid",
  "create": "auth != null && auth.loginType != 'ANONYMOUS' && doc._id == auth.uid && doc.ownerId == auth.uid",
  "update": "auth != null && auth.loginType != 'ANONYMOUS' && doc._id == auth.uid",
  "delete": "auth != null && auth.loginType != 'ANONYMOUS' && doc._id == auth.uid"
}
